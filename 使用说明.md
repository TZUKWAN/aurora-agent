# 商业计划书撰写系统 - 使用说明

## 概述

本系统专注于高质量商业计划书的撰写，采用您在 `D:\计划书AI` 中使用的专业76章节大纲和精细的提示词，结合python-docx进行Word文档构建。

## 文件结构

```
aurora-agent/
├── business_plan_writer.py    # 核心功能模块
├── cli.py                     # 命令行接口
├── outline_with_prompts.json  # 专业大纲（76章节）
├── 使用说明.md               # 本文件
└── requirements.txt           # 依赖库
```

## 安装依赖

```bash
pip install python-docx
```

## 快速开始

### 1. 初始化项目

```bash
python cli.py init --product "您的产品名称" --company "您的公司名称" --desc "项目描述"
```

这将在 `output/content_plan.json` 中生成一个内容计划，包含所有章节的专业提示词。

### 2. 查看大纲信息

```bash
python cli.py info
```

查看大纲的章节结构和概要。

### 3. 撰写内容

编辑 `output/content_plan.json`，将您撰写的内容填入每个章节的 `generated_content` 字段。

### 4. 构建Word文档

```bash
python cli.py build --input output/content_plan.json --output 商业计划书.docx
```

使用python-docx构建最终的Word文档。

## 核心特性

1. **专业大纲** - 76章节的精细结构，覆盖商业计划书的所有必要部分
2. **高质量提示词** - 每个章节都有专业、详细的写作指引
3. **python-docx文档构建** - 使用稳定可靠的python-docx库
4. **中文字体支持** - 内置宋体、黑体等中文字体支持

## 核心类说明

### BusinessPlanWriter

负责：
- 加载和管理大纲
- 创建项目信息
- 生成内容计划（JSON格式）
- 保存和加载内容计划

### DocxBuilder

负责：
- 使用python-docx构建Word文档
- 处理标题、段落、表格、图片等元素
- 支持中文字体和格式设置

## 与原aurora-agent的区别

| 特性 | 原系统 | 优化后 |
|------|--------|--------|
| 重点 | 多智能体竞赛助手 | 专业商业计划书撰写 |
| 章节数 | 10个通用章节 | 76个专业章节 |
| 提示词 | 简单模板 | 精细、专业的写作指引 |
| 文档构建 | python-docx库（简单） | python-docx库（专业格式） |

## 依赖库

仅需：
- `python-docx` - Word文档处理
